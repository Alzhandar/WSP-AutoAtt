services:
  wsp-autoattend:
    build: .
    ports:
      - "8080:8000"
    environment:
      - TZ=Asia/Almaty
      - DISPLAY=:99
    restart: unless-stopped
    volumes:
      - ./chrome_sessions:/app/chrome_sessions